import { CompSuccsessDirective } from './comp-succsess.directive';

describe('CompSuccsessDirective', () => {
  it('should create an instance', () => {
    const directive = new CompSuccsessDirective();
    expect(directive).toBeTruthy();
  });
});
