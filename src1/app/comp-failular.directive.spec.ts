import { CompFailularDirective } from './comp-failular.directive';

describe('CompFailularDirective', () => {
  it('should create an instance', () => {
    const directive = new CompFailularDirective();
    expect(directive).toBeTruthy();
  });
});
